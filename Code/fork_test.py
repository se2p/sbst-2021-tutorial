import numpy as np

LANE_WIDTH=4.0
delta = np.random.normal(0, LANE_WIDTH * 0.5, 1)[0]
delta = np.random.normal(0, LANE_WIDTH * 0.5, 1)
delta = np.random.normal(0, LANE_WIDTH * 0.5, 1)
delta = np.random.normal(0, LANE_WIDTH * 0.5, 1)

